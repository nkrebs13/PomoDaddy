disabled_rules:
  - force_unwrapping      # Force unwrap is common in tests
  - explicit_top_level_acl # Tests don't need explicit access control
  - explicit_type_interface # Tests don't need explicit type annotations
  - explicit_init          # Tests can use .init() freely
  - file_header            # Test files don't need standardized headers
